const styles = {
    container: 'font-serif text-5xl text-center py-10 drop-shadow'
}

const Header = () => {
    return (
        <div className={ styles.container }>
            <header>Alexander Spurlock</header>
        </div>
    )
}

export default Header