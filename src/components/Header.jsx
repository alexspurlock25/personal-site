import * as React from 'react'

const containerStyles = 'font-serif text-5xl text-center py-10 drop-shadow'

const Header = () => {
    return (
        <div className={ containerStyles }>
            <header>Alexander Spurlock</header>
        </div>
    )
}

export default Header
